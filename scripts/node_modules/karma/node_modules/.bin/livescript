#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../LiveScript/bin/livescript" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../LiveScript/bin/livescript" "$@"
  ret=$?
fi
exit $ret
