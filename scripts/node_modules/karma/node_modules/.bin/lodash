#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../lodash/build.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../lodash/build.js" "$@"
  ret=$?
fi
exit $ret
