#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../LiveScript/bin/slake" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../LiveScript/bin/slake" "$@"
  ret=$?
fi
exit $ret
