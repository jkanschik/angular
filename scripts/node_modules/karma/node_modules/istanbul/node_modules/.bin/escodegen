#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../escodegen/bin/escodegen.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../escodegen/bin/escodegen.js" "$@"
  ret=$?
fi
exit $ret
