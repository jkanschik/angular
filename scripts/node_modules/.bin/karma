#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../karma/bin/karma" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../karma/bin/karma" "$@"
  ret=$?
fi
exit $ret
